fn main() {
    println!("Hello, cargo!");
}

//
// Learned so far:
//

// Can create new projects using `cargo new`
// Can build (compile) using `cargo build`
// Can build and run using`cargo run`
// Can compile (without creating the binary) using `cargo check`
